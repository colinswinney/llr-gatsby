import styled from 'styled-components'
import { vars } from '../../../../styles/global'

export const ContainerEl = styled.div`
    margin: auto;
    width: 100%;
    max-width: ${vars.breakpointLg};
`